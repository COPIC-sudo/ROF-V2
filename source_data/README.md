# Source data

Source data are deposited separately as:

```text
ROF_results_v1_1_integrated_evidence_lock.zip
```

This code repository intentionally does not embed the results archive. Pass the archive or extracted evidence-lock root explicitly to the scripts in `figure_tools/`.

Before publication, replace this note with the permanent data-repository DOI and access URL.
